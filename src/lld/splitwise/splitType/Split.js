export class Split {
    user;
    amount;

    Split(user) {
        this.user = user;
    }
    
    setAmount(amount) {
        this.amount = amount;
    }

    getUser() {
        return user;
    }
}